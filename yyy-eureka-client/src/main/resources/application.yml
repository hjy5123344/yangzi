server:
  port: 8762

spring:
  application:
    name: eureka-client
  redis:
    host: 112.74.29.31
    port: 6379
    database: 0
    jedis:
      pool:
        max-active: 50
        max-wait: 3000
        max-idle: 20
        min-idle: 2
    timeout: 5000
    password: 123456

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

